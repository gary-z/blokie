#pragma once
// Canonical golden header moved to engine/golden/golden_common.h
// This shim kept for backwards compatibility — new code should include "golden_common.h" via engine/golden.
#include "../golden/golden_common.h"
